rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /adventures/{adventureId}/memories/{memoryId}/{mediaId} {
      allow read, write:
        if request.auth != null;
    }
  }
}
